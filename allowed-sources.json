{
  "about": "What a page made by create-page may load, and what its script may build at runtime. scripts/check-ideas.ts holds every example to this file, and the pages worker in instrument-org/internal derives the connect-src of every hosted copy from it, so an origin added here needs a pages deploy there before hosted copies honor it. These pages are written by an agent reaching for whatever a page needs, so the list names the general-purpose package hosts rather than the packages we happened to think of: any library on any of them is fair game. What every entry does insist on is an exact version, since a page is an artifact someone opens in a year and an unpinned URL resolves to whatever is newest that day. Tags are matched by exact origin and path family, so a lookalike host does not pass; a pin is a pattern the path must also match.",
  "tags": [
    {
      "origin": "https://fonts.googleapis.com",
      "path": "/",
      "why": "The type: one stylesheet naming the faces every page sets."
    },
    {
      "origin": "https://fonts.gstatic.com",
      "path": "/",
      "why": "The font files that stylesheet points at."
    },
    {
      "origin": "https://tryinstrument.com",
      "path": "/page.js",
      "why": "The share widget every page carries."
    },
    {
      "origin": "https://esm.sh",
      "path": "/",
      "pin": "^\\/(?:@[\\w.-]+\\/)?[\\w.-]+@\\d+\\.\\d+\\.\\d+(?:[-+][\\w.-]+)?(?:\\/|$)",
      "why": "The default, and where the starter's own framework and icon set come from. esm.sh serves any npm package as a module at one URL shape, resolves a React once across a whole import graph when asked (?deps=), hands a file over untouched when asked (?raw), and marks a pinned URL immutable. Reach past it only for a package it cannot build, or a build only another host carries."
    },
    {
      "origin": "https://cdn.jsdelivr.net",
      "path": "/npm/",
      "pin": "^\\/npm\\/(?:@[\\w.-]+\\/)?[\\w.-]+@\\d+\\.\\d+\\.\\d+(?:[-+][\\w.-]+)?(?:\\/|$)",
      "why": "Mirrors npm as published, so it carries the UMD and IIFE builds a package ships and esm.sh would rewrite, and it is the host older shared pages were written against."
    },
    {
      "origin": "https://unpkg.com",
      "path": "/",
      "pin": "^\\/(?:@[\\w.-]+\\/)?[\\w.-]+@\\d+\\.\\d+\\.\\d+(?:[-+][\\w.-]+)?(?:\\/|$)",
      "why": "The other npm mirror, and the one a package's own README usually names, so an agent copying a library's documented script tag lands here."
    },
    {
      "origin": "https://cdnjs.cloudflare.com",
      "path": "/ajax/libs/",
      "pin": "^\\/ajax\\/libs\\/[\\w.-]+\\/\\d+\\.\\d+\\.\\d+(?:[-\\w.]+)?\\/",
      "why": "Hand-curated rather than a mirror, which is why it carries libraries that never shipped to npm and the minified single-file builds of ones that did."
    }
  ],
  "built": [
    {
      "prefix": "https://t0.gstatic.com",
      "why": "A link wears the icon of the site it points at, and no CSS can read a host out of an href, so the URL is built from the link."
    },
    {
      "prefix": "https://tile.openstreetmap.org/",
      "why": "Map tiles are a URL template a map library fills in per tile. A page whose tiles never arrive still carries its places as a list, which is the rule the map template is built around."
    },
    {
      "prefix": "https://www.openstreetmap.org/copyright",
      "why": "The attribution a map library writes into its corner, as a link."
    }
  ]
}
