{
  "name": "@instrument-org/skill-powerpoint",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "check:skill": "node ../../scripts/check-skill.ts",
    "test": "uv run --no-project --with pytest --with python-pptx --with Pillow --with pymupdf pytest tests/ -v -m 'not slow'"
  }
}
