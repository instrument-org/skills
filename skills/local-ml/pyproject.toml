[project]
name = "skill-local-ml"
version = "0.0.0"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
test = ["pytest", "Pillow"]
slow = ["transformers", "torch", "sentence-transformers", "rembg[cpu]", "numba>=0.60", "openai-whisper"]

[tool.pytest.ini_options]
markers = ["slow: requires model downloads (~100MB-2GB), excluded from default run"]
