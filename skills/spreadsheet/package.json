{
  "name": "@instrument-org/skill-spreadsheet",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "check:skill": "node ../../scripts/check-skill.ts",
    "test": "uv run --no-project --with pytest --with openpyxl --with pandas pytest tests/ -v -m 'not slow'"
  }
}
