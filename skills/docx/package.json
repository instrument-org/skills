{
  "name": "@instrument-org/skill-docx",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "check:skill": "node ../../scripts/check-skill.ts",
    "check:python": "uv run --locked --project . --extra test python -m compileall -q scripts tests",
    "test": "uv run --locked --project . --extra test pytest tests/ -v -m \"not slow\""
  }
}
