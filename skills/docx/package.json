{
  "name": "@instrument-org/skill-docx",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "check:skill": "node ../../scripts/check-skill.ts",
    "test": "uv run --no-project --with pytest --with python-docx --with docxtpl pytest tests/ -v -m 'not slow'"
  }
}
